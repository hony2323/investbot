# Copy to .env and fill in. .env is git-ignored.

# Anthropic API key for Claude (required for `analyze` and `stock`).
ANTHROPIC_API_KEY=

# Interactive Brokers TWS / IB Gateway API connection.
# Defaults target a local paper-trading TWS instance.
IB_HOST=127.0.0.1
IB_PORT=7497
IB_CLIENT_ID=17
