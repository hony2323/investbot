# investbot-api

FastAPI service that wraps the `investbot` library. Read-only and advisory — it never
places trades. See the repo root README for how to run it (`make api`).

Endpoints live under `/api`; interactive docs at `/docs`.
